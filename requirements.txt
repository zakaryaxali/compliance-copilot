# Source-document parsing (build_source_index.py)
pypdf>=4.0.0

# Dataset generation + inference API client
# (Together is OpenAI-compatible; we point the OpenAI SDK at Together's URL.)
openai>=1.20.0

# Optional fallback inference backend
huggingface_hub>=0.23.0

# Training config
pyyaml>=6.0

# MLX fine-tune + inference (Apple Silicon only; install separately on Mac):
#   pip install mlx-lm
# Pinned here in a comment so the requirements file works on non-Mac
# environments (e.g. CI) where mlx-lm cannot install.
